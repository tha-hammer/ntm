version: "2"
run:
  modules-download-mode: readonly
  issues-exit-code: 1
  tests: true
output:
  formats:
    text:
      path: stdout
      print-linter-name: true
      print-issued-lines: true
linters:
  enable:
    - bodyclose
    - dogsled
    - durationcheck
    - gosec
    - nakedret
    - nolintlint
    - rowserrcheck
    - sqlclosecheck
  disable:
    - dupl
    - exhaustive
    - funlen
    - gochecknoglobals
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - godot
    - goprintffuncname
    - ineffassign
    - lll
    - misspell
    - noctx
    - prealloc
    - predeclared
    - revive
    - thelper
    - tparallel
    - unconvert
    - unparam
    - unused
    - wastedassign
    - whitespace
    - wsl
  settings:
    errcheck:
      check-type-assertions: false
      check-blank: false
      exclude-functions:
        - (io.Closer).Close
        - (net/http.ResponseWriter).Write
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Print
        - fmt.Printf
        - fmt.Println
        - (os.File).Close
        - (*os.File).Close
    gosec:
      excludes:
        - G104
        - G110
        - G115
        - G118  # context cancel: noisy on intentionally-leaked contexts
        - G122  # WalkDir TOCTOU: would force os.Root refactor across codebase
        - G202  # SQL string concat: false-positive prone
        - G204
        - G301
        - G302
        - G304
        - G305
        - G306
        - G404
        - G702  # int overflow conversion: noisy on size_t-style code
        - G703  # taint-analysis path traversal: 26 false positives, paths already validated
        - G704  # taint-analysis SSRF: URLs come from trusted config
        - G706  # filepath.Walk TOCTOU: same class as G122
    govet:
      disable:
        - shadow
        - fieldalignment
        - unusedwrite
        - inline  # noisy auto-fixable hints (ioutil migration, reflect const inlining)
      enable-all: true
    nakedret:
      max-func-lines: 50
  exclusions:
    generated: lax
    rules:
      - linters:
          - dogsled
          - errcheck
          - gosec
          - rowserrcheck
          - sqlclosecheck
          - staticcheck
        path: _test\.go
      - linters:
          - errcheck
        path: internal/cli/
      - linters:
          - errcheck
        path: internal/(checkpoint|agentmail|cass|bv|auth|tmux|config|robot|events|history|notify|output|quota|tui|status|scanner|prompt|state|supervisor|tools|util|agent|handoff)/
      - linters:
          - dogsled
          - errcheck
          - rowserrcheck
          - sqlclosecheck
        path: tests/
      - linters:
          - staticcheck
        text: 'SA1019:'
      - linters:
          - staticcheck
        text: 'SA9003:'
      - linters:
          - staticcheck
        text: 'SA4017:'
      - linters:
          - staticcheck
        text: 'SA5011:'
      - linters:
          - staticcheck
        text: 'QF1012:'  # perfsprint: use WriteString instead of fmt.Fprintf — stylistic, 326 instances
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  new: false
severity:
  default: warning
  rules:
    - linters:
        - govet
      severity: error
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - github.com/Dicklesworthstone/ntm
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
