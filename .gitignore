# Binaries (only at root, not cmd/ntm/)
/ntm
*.exe
dist/

# Test coverage
coverage.out
coverage.html

# IDE
.idea/
.vscode/
web/node_modules/
web/.next/
web/coverage/
vscode/node_modules/
vscode/out/
vscode/package-lock.json
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
._*

# Go
vendor/

# Test binaries
*.test
coverage.out
dashboard_debug.txt

# Debug output files
internal/cli/outputs/

# Local scratch SQLite DBs (never tracked)
storage.sqlite3
storage.sqlite3-*

# bv (beads viewer) local config and caches
.bv/
*.out
.ntm/

# beads local state
.beads/.br_history/
.beads/.bv.lock
.beads/.write.lock
.beads/.sync.lock
.beads/.migration-hint-ts

# Temporary/backup files
*.bak
*.incomplete
*.jsonl.lock

# Go build temp directories
.tmp-go/

# Coverage reports
cli_coverage.txt

# Screenshots
Snipping_*.png

# Ephemeral/temporary files (agent workflow artifacts)
RESEARCH_FINDINGS.md
TOON_INTEGRATION_BRIEF.md
cov_*.out
rustc-ice-*.txt
*.snap.new
sweep_*.png
test_screenshot.png
claude-upgrade-progress.json

# E2E runtime lockfiles
e2e/*.lock

# Go test coverage profiles
*.cover

# E2E test log files
scripts/test-label-e2e-*.log

# Agent coordination config
.agent-mail.yaml

# Ephemeral files (core dumps, profiling, agent config backups, etc.)
core.*
core
perf.data
perf.data.old
*.bak
clippy.txt
clippy.json
clippy_results.json
clippy_unwrap.json
clippy_unwrap_clean.txt
check_output.txt
error.log
.next_backup*/
cline.mcp.json
opencode.json
windsurf.mcp.json
*.mcp.json.*.bak
storage.sqlite3
storage.sqlite3-shm
storage.sqlite3-wal
storage.sqlite3.bak
storage.sqlite3.corrupt-*
storage.sqlite3-shm.corrupt-*
storage.sqlite3-wal.corrupt-*
.env
.claude/
.gomodcache/

# Go build cache (NEVER commit this — an agent did once, 3500+ files, 100+ MB)
.gocache/

# Beads recovery/snapshot artifacts (ephemeral debugging state)
.beads/recovery_*/
.beads/recovery_snapshot_*/
.beads_snapshot_*/

# Ephemeral debug output files at repo root
/*.txt
/*.py

# Agent scratch/debug test files (machine-local, never tracked)
**/zz_live_debug_test.go
